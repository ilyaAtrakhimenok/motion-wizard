import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
	wrapper: {
		flex: 1,
		justifyContent: 'center',
		alignItems: 'center',
		gap: 20,
	},
	sheet: {
		width: 200,
		height: 200,
		backgroundColor: 'red',
		borderRadius: 20,
	},
	title: {
		textAlign: 'center',
		fontWeight: '600',
		fontSize: 48,
		marginTop: 100,
	},
});
